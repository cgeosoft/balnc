{{>_header}}
<section class="bg-primary pb-4">
    {{>navbar}}
    <div class="container">
      <h1 class="displa-4 text-white">{{title}}</h1>
    </div>
</section>
<section class="bg-light pt-4">
    <div class="container">
        {{{contents}}}
    </div>
</section>
{{>footer}}
{{>_footer}}